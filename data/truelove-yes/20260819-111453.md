# truelove-yes/20260819-111453

## Resumen

El modelo `truelove-yes/20260819-111453` es un modelo de lenguaje de gran escala basado en una arquitectura Mixture of Experts (MoE) de la familia Qwen3.5, desarrollado por el usuario `truelove-yes` y publicado en HuggingFace. Se construye sobre el modelo base `vera6/affine-5g4yyz75zuz-t6`, que según sus etiquetas corresponde a un modelo de imagen-texto a texto, lo que sugiere una posible capacidad multimodal, aunque no se confirma en la documentación disponible. Con 35.107 millones de parámetros totales, el modelo está diseñado para generación de texto y razonamiento, con indicios de un ajuste mediante DPO (offline-dpo) y una variante de razonamiento (reason-v4). El acceso es restringido (gated) y la licencia es Apache 2.0. A fecha de creación (agosto de 2026) no tiene descargas ni usos registrados, lo que indica que es un modelo reciente y sin validación pública.

## Especificaciones técnicas

| Parametro | Valor |
|---|---|
| Arquitectura | Qwen3.5 MoE (Mixture of Experts) |
| Parametros totales | 35.107.181.936 (35,1 B) |
| Parametros activos | no disponible |
| Longitud de contexto | no disponible |
| Tipos de cuantizacion | no disponible (pesos en safetensors) |
| Idiomas soportados | no disponible |
| Licencia | Apache 2.0 |
| Formato de pesos | safetensors |

## Arquitectura y entrenamiento

La arquitectura se identifica como Qwen3.5 MoE, un modelo de mezcla de expertos que activa solo una fracción de sus parámetros por token. Las etiquetas incluyen `affine`, `sn120`, `reason-v4`, `offline-dpo` y `r861`, lo que sugiere que se aplicó una optimización de preferencias directa (DPO) en algún momento del entrenamiento y que existe una variante de razonamiento. El modelo base es `vera6/affine-5g4yyz75zuz-t6`, que según su nombre pertenece a la familia `affine` y tiene capacidades de imagen-texto a texto, lo que podría implicar que el modelo final hereda cierta capacidad multimodal, aunque no está documentado. No se dispone de información sobre la cantidad de tokens de entrenamiento, la composición del dataset, ni las técnicas de alineación adicionales. Tampoco se especifican detalles sobre la atención, el número de expertos, ni la estrategia de decodificación.

## Capacidades

No se ha publicado una descripción oficial de las capacidades del modelo. A partir de las etiquetas y del modelo base, se puede inferir potencialmente lo siguiente, pero sin confirmación:

- Generación de texto y razonamiento (por la etiqueta `reason-v4`).
- Posible procesamiento de imágenes, dado que el modelo base es `image-text-to-text`, aunque el pipeline declarado es `text-generation`.
- Ajuste por preferencias humanas mediante DPO, lo que podría mejorar la calidad de las respuestas.
- No se confirma soporte de tool calling, funciones de agente, ni capacidades multilingües específicas.

En ausencia de documentación, no se pueden afirmar capacidades concretas.

## Casos de uso

No se han publicado casos de uso oficiales ni documentación de aplicaciones prácticas. Dado que el modelo es de acceso restringido y no tiene validación pública, no se recomienda su uso en producción sin una evaluación previa. Cualquier caso de uso sería especulativo y no se puede respaldar con datos verificados. Por tanto, no se listan casos de uso concretos en esta ficha.

## Benchmarks y rendimiento

No se han publicado resultados de benchmarks en la información disponible. No se puede comparar su rendimiento con otros modelos ni afirmar ninguna métrica.

## Requisitos de hardware

El repositorio ocupa 70,2 GB en formato safetensors, lo que indica que los pesos en precisión FP16/FP32 ocupan aproximadamente esa cantidad. Para inferencia con todos los pesos en memoria, se requiere:

- **VRAM estimada**: al menos 70 GB para FP16, o 35 GB si se cuantiza a 8 bits, aunque no se especifican cuantizaciones disponibles.
- **GPU recomendadas**: una GPU de 80 GB como A100 o H100, o múltiples GPUs de 40 GB (A100) en paralelo.
- **Opciones de despliegue**: no se indican opciones específicas, pero al ser compatible con transformers, se podrían usar vLLM, TGI o llama.cpp si se generan pesos GGUF, aunque no se confirma su compatibilidad.
- **Latencia y throughput**: no disponibles.

## Comparativa con modelos similares

No se dispone de información sobre modelos similares con los que comparar directamente. El modelo se basa en Qwen3.5 MoE, pero no se conocen sus características exactas ni su rendimiento. No se puede establecer una comparativa con Mixtral 8x7B, Qwen3 MoE u otros modelos de tamaño similar sin datos de benchmarks.

## Limitaciones y advertencias

- **Acceso restringido**: el modelo es gated, por lo que se requiere aceptar condiciones en HuggingFace antes de poder descargarlo.
- **Falta de documentación**: no hay documentación técnica, papers ni guías de uso.
- **Sin validación pública**: no tiene descargas ni likes, lo que indica que no ha sido probado por la comunidad.
- **Posibles sesgos y alucinaciones**: al ser un modelo sin evaluación pública, no se conocen sus sesgos ni su tasa de alucinación.
- **Limitaciones de contexto e idioma**: se desconocen los límites de contexto y los idiomas soportados.
- **Uso comercial**: la licencia Apache 2.0 permite uso comercial, pero el acceso restringido puede implicar términos adicionales que deben revisarse antes de su uso.

## Enlaces

- [Modelo en HuggingFace](https://huggingface.co/truelove-yes/20260819-111453)

No se han encontrado otros enlaces relevantes (papers, blogs, repos) en la búsqueda web realizada.
